# malbolge-linguist — Malbolge support for github-linguist

Workspace **PR-ready in structure, not yet eligible for submission** to `github-linguist/linguist`. Built `EVIDENCE BEFORE NARRATIVE` - no invented usage, no Hello World samples, no unpublished grammar claimed as done.

- **Upstream**: `github-linguist/linguist@d5214e1612c858ba14bf98edeca57e1683276f1d` (clone in `upstream/`, gitignored)
- **Branch**: `malbolge-linguist` (this repo, isolated from `C:\Development\ISyCo`)
- **Color**: `#1D1A2F` — EVA-01 deep purple
- **Extension**: `.malbolge` only (`.mal` rejected, see below)

## Status — `docs/FINAL_AUDIT.md` (re-audit 2026-09-02)

```
TECHNICAL_IMPLEMENTATION_READY = TRUE
SAMPLE_READY = TRUE
GRAMMAR_READY = TRUE
DETECTION_READY = TRUE
NO_NEW_FAILURES = TRUE
TECHNICAL_WORK = DONE
USAGE_GATE = NOT_DEMONSTRATED
POLICY_ELIGIBLE = FALSE
PR_READY = FALSE
PR_OPEN = FALSE
MAINTAINER_GUIDANCE = RECEIVED
SEMANTIC_ALTERNATIVE_PATH = REJECTED
USAGE_ASSESSMENT = PER_EXTENSION_OR_FILENAME
FINAL_STATE = BLOCKED_BY_USAGE_POLICY
```

Discussion #8164 received maintainer guidance:
https://github.com/github-linguist/linguist/discussions/8164

`@lildude`: "Usage has to be supported per extension or filename being added."

Legacy: `LANGUAGE_DEFINITION_READY=TRUE`, `SAMPLES_READY=TRUE` (was BLOCKED, now TRUE via PR-authored MIT sample), `DETECTION_READY` `NOT_DEMONSTRATED`→`TRUE` post-patch, `TESTS_PASS`→`NO_NEW_FAILURES=TRUE` (2/1 vs 2/1), `USAGE_GATE` stays `NOT_DEMONSTRATED`.

**Do not open PR** - maintainer guidance confirms that `POLICY_ELIGIBLE=FALSE` for the proposed `.malbolge` extension. The blocker is usage policy, not technical readiness.

Historical/project epilogue: [`docs/EPILOGUE.md`](docs/EPILOGUE.md).

## Why STILL BLOCKED (policy)

1. **Usage gate** (`CONTRIBUTING.md:236-250`): 2000 files (common extension) / 200 (once-per-repo) last year, forks excluded, distributed. Measured 2026-09-02 `NOT is:fork`:
   - `extension:malbolge NOT is:fork` = **42** (3 with term Malbolge)
   - `extension:mal NOT is:fork` = **6096** but only 27 Malbolge, heavily contaminated (Malabar, Scheme, model)
   - Verdict: `NOT_DEMONSTRATED`. No honest search meets threshold. See `evidence/github_usage/results_summary.json`. **No DannyBaanks-owned repo counted**; no mass creation; `CLAIM_SCOPE <= EVIDENCE_SCOPE`.

2. **Grammar**: `GRAMMAR_PACKAGE_READY=TRUE` (published `https://github.com/DannyBaanks/malbolge-syntax` `225f5acb`, MIT, `syntaxes/` layout) and `GRAMMAR_IN_FINAL_UPSTREAM_PATCH=TRUE` (integrated via **canonical** `script/add-grammar https://github.com/DannyBaanks/malbolge-syntax` exit 0 → submodule `vendor/grammars/malbolge-syntax` + `grammars.yml` `source.malbolge` + license cache + `vendor/README.md` via `script/list-grammars`; `test_all_languages_have_grammars` and `test_readme_file_is_in_sync` pass). See `grammar/malbolge-syntax/README.md` and `evidence/patches/malbolge.patch`.

3. **History**: PR #4609 (2019, `.mb`, closed not merged, see `docs/HISTORY_PR4609.md`) closed for same usage reason (81k `.mb` but only 2 Hello Worlds). Current proposal **does not repeat** `notability=usage`.

## What is ready (re-audit)

- **Patch** canonical commit `1af6aef9` (Docker volume `malbolge-canonical-work` `/workspace/patched`, branch `malbolge-canonical`, base `d5214e16`; not pushed): `languages.yml` entry (Mako/Markdown alphabetical, `#1D1A2F`, `.malbolge`, `tm_scope: source.malbolge`, `language_id: 1006177966`), `samples/Malbolge/truth_machine.malbolge` (254 B, SHA `7062713e...`, MIT, mode 100644, see `docs/SAMPLE_PROVENANCE.md`), reproducible patch `evidence/patches/malbolge.patch` (SHA `DFDA0517F53F95209DEEC4D43A6E287EBDC20C846D14034BFEDFAB57BE4620E8`, `git format-patch d5214e16..1af6aef9`, `git apply` verified on a fresh clone).

- **Sample** `SAMPLE_READY=TRUE`: `truth_machine.malbolge` — `IN→branch→OUT→HALT/loop` Classic, `'0'`→halt 136 steps, `'1'`→loop, cross-verified `gost`+`oracle`, not Hello World, MIT provenance (PR-authored-sample clause, `CONTRIBUTING.md:150-161`).

- **Grammar** `GRAMMAR_PACKAGE_READY=TRUE` (published `https://github.com/DannyBaanks/malbolge-syntax` `225f5acb`, `syntaxes/` layout), `GRAMMAR_IN_FINAL_UPSTREAM_PATCH=TRUE` (integrated via **canonical** `script/add-grammar` exit 0 — submodule, `grammars.yml`, license cache, `vendor/README.md` all generated by upstream tooling, not hand-written). Conservative (does not mislabel positional opcodes).

- **Detection** `DETECTION_READY=TRUE` post-patch (`evidence/canonical/detection.log`): `FileBlob` → `language=Malbolge`, `extname=.malbolge`, `mime=text/plain`; `Language["Malbolge"]` → `ext:[".malbolge"] color:#1D1A2F id:1006177966 tm_scope:source.malbolge`; end-to-end `github-linguist --breakdown` → `100.00% 254 Malbolge` (`breakdown_cli.log`).

- **Tests** `NO_NEW_FAILURES=TRUE` (same Docker environment, `evidence/canonical/`): baseline `rake_test.log` 2034 runs 39353 assertions **2 failures 1 error** vs patched `rake_test_final.log` 2036 runs 39369 assertions **2 failures 1 error** (+2 runs Malbolge sample, +16 assertions, **0 new failures**; the 2 failures+1 error are CodeMirror-only, `vendor/CodeMirror` absent from a fresh clone, unrelated to Malbolge).

- Contamination: `.mal` false positives (`corkami/mitra` Malabar, `larcenists/larceny` Scheme) — `evidence/github_usage/sample_repositories.md`; heuristics none for `.malbolge`.

Canonical pipeline (Docker): volume `malbolge-canonical-work` (`baseline/` pinned `d5214e16`, `patched/` branch `malbolge-canonical`), gem bundle volume `malbolge-canonical-bundle`, DIND `malbolge-canonical-dind`, runner `malbolge-linguist-addgrammar`. Full raw logs in `evidence/canonical/`.

## Structure

```
malbolge-linguist/
  evidence/
    github_usage/          # queries.md, results_summary.json, sample_repositories.md
    canonical/             # canonical pipeline logs (baseline 2034/2/1, patched 2036/2/1), add_grammar.log, detection.log, breakdown_cli.log, apply_verify.log, environment.log
    tests/                 # legacy non-canonical logs (preserved for history)
    patches/malbolge.patch # canonical, d5214e16..1af6aef9, SHA DFDA0517...
  grammar/malbolge-syntax/ # LICENSE MIT, malbolge.tmLanguage.json (source.malbolge), package.json, tests/sample.malbolge + validation.md, README
  PR_DRAFT.md              # mirrors .github/PULL_REQUEST_TEMPLATE.md, unchecked where blocked
  TOOLCHAIN_LOCK.json
  README.md                # this file
```

Canonical patch commit `1af6aef9` lives in the Docker volume (`malbolge-canonical-work/patched`, not pushed); research docs never enter final patch. Patch when policy allows:

```
lib/linguist/languages.yml  # source.malbolge
samples/Malbolge/truth_machine.malbolge
grammars.yml + vendor/grammars/malbolge-syntax (via script/add-grammar)
vendor/README.md (via script/list-grammars)
vendor/licenses/git_submodule/malbolge-syntax.dep.yml
```

## For the auditor (GPT)

```powershell
gh repo clone DannyBaanks/malbolge-linguist
cd malbolge-linguist
git log --oneline -5
cat docs/FINAL_AUDIT.md
cat docs/LINGUIST_REQUIREMENTS.md
cat evidence/github_usage/results_summary.json
cat evidence/tests/fileblob.log
cat SHA256SUMS.txt; py -c "import hashlib,pathlib;root=pathlib.Path('.');print(all(hashlib.sha256(p.read_bytes()).hexdigest()==open('SHA256SUMS.txt').read().split()[i*2] for i,p in enumerate(sorted([p for p in root.rglob('*') if p.is_file() and '.git' not in p.parts and p.name!='SHA256SUMS.txt' and 'upstream' not in p.parts]))))"
```

Check: `upstream/CONTRIBUTING.md:134-250` vs `PR_DRAFT.md`; `evidence/github_usage/queries.md` vs live GitHub search; `grammar/malbolge-syntax/Malbolge.tmLanguage.json` JSON validity.

## Next steps (only policy remains)

1. ~~Publish `grammar/malbolge-syntax`~~ Done (`https://github.com/DannyBaanks/malbolge-syntax` `225f5acb`, `syntaxes/` layout, canonical `script/add-grammar` exit 0, patch `evidence/patches/malbolge.patch` `DFDA0517...`)
2. ~~Sample~~ Done (`SAMPLE_READY=TRUE`, `truth_machine.malbolge` `70627...`)
3. ~~ProjectMap census~~ Done (demo `evidence/census/corpus` 3 files: baseline `verified 1` → with `external.json` `verified 2 rejected 1`; extrapolating GitHub Search `VALID_MALBOLGE` ~71 `UNIQUE_REPOS` ~40-45 < 2000 → `USAGE_GATE=NOT_DEMONSTRATED` final, see `evidence/census/README.md`)
4. **Discussion #8164 resolved the policy question:** usage must be supported per proposed extension or filename. No further action is planned.

## Links

- Repo: `https://github.com/DannyBaanks/malbolge-linguist`
- Upstream: `https://github.com/github-linguist/linguist`
- Genealogy source (read-only): `C:\Development\ISyCo Git\GENEALOGIA_MALBOLGE`

*EVIDENCE BEFORE NARRATIVE. No usage invented. EVA-01 #1D1A2F.*
